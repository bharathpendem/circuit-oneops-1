name: manage-azure-policy-03ed1123
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'initiatives/Initiative_-_NHC_Policy_Prod_Inherit_Tags_fromRG_To_Resources_3e69822ce8e64e0b98f19fe4/**'
#      - .github/workflows/manage-azure-policy-03ed1123.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_03ed1123}}
        allow-no-subscriptions: true
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          initiatives/Initiative_-_NHC_Policy_Prod_Inherit_Tags_fromRG_To_Resources_3e69822ce8e64e0b98f19fe4/**
